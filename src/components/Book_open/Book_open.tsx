import React from 'react';
import type { FC } from 'react';
import type { Book_openProps } from './Book_open.types';

export const Book_open: FC<Book_openProps> = props => {
  return <div>{'Book_open stub'}</div>;
};
