import React from 'react';
import type { FC } from 'react';
import type { Mic_offProps } from './Mic_off.types';

export const Mic_off: FC<Mic_offProps> = props => {
  return <div>{'Mic_off stub'}</div>;
};
