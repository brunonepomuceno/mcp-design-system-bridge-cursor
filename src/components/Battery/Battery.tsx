import React from 'react';
import type { FC } from 'react';
import type { BatteryProps } from './Battery.types';

export const Battery: FC<BatteryProps> = props => {
  return <div>{'Battery stub'}</div>;
};
