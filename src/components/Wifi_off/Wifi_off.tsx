import React from 'react';
import type { FC } from 'react';
import type { Wifi_offProps } from './Wifi_off.types';

export const Wifi_off: FC<Wifi_offProps> = props => {
  return <div>{'Wifi_off stub'}</div>;
};
