import React from 'react';
import type { FC } from 'react';
import type { RewindProps } from './Rewind.types';

export const Rewind: FC<RewindProps> = props => {
  return <div>{'Rewind stub'}</div>;
};
