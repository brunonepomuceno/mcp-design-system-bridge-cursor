import React from 'react';
import type { FC } from 'react';
import type { SquareProps } from './Square.types';

export const Square: FC<SquareProps> = props => {
  return <div>{'Square stub'}</div>;
};
