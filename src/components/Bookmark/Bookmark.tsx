import React from 'react';
import type { FC } from 'react';
import type { BookmarkProps } from './Bookmark.types';

export const Bookmark: FC<BookmarkProps> = props => {
  return <div>{'Bookmark stub'}</div>;
};
