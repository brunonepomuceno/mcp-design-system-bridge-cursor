import React from 'react';
import type { FC } from 'react';
import type { CircleProps } from './Circle.types';

export const Circle: FC<CircleProps> = props => {
  return <div>{'Circle stub'}</div>;
};
