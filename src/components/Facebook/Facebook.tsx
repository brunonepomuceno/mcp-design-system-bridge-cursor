import React from 'react';
import type { FC } from 'react';
import type { FacebookProps } from './Facebook.types';

export const Facebook: FC<FacebookProps> = props => {
  return <div>{'Facebook stub'}</div>;
};
