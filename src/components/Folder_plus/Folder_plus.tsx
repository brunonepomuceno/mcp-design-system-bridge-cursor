import React from 'react';
import type { FC } from 'react';
import type { Folder_plusProps } from './Folder_plus.types';

export const Folder_plus: FC<Folder_plusProps> = props => {
  return <div>{'Folder_plus stub'}</div>;
};
