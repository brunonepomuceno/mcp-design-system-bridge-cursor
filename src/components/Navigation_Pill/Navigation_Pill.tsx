import React from 'react';
import type { FC } from 'react';
import type { Navigation_PillProps } from './Navigation_Pill.types';

export const Navigation_Pill: FC<Navigation_PillProps> = props => {
  return <div>{'Navigation_Pill stub'}</div>;
};
