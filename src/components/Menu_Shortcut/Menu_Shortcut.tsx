import React from 'react';
import type { FC } from 'react';
import type { Menu_ShortcutProps } from './Menu_Shortcut.types';

export const Menu_Shortcut: FC<Menu_ShortcutProps> = props => {
  return <div>{'Menu_Shortcut stub'}</div>;
};
