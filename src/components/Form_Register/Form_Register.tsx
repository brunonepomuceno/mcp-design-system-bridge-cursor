import React from 'react';
import type { FC } from 'react';
import type { Form_RegisterProps } from './Form_Register.types';

export const Form_Register: FC<Form_RegisterProps> = props => {
  return <div>{'Form_Register stub'}</div>;
};
