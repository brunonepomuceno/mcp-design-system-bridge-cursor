import React from 'react';
import type { FC } from 'react';
import type { FramerProps } from './Framer.types';

export const Framer: FC<FramerProps> = props => {
  return <div>{'Framer stub'}</div>;
};
