import React from 'react';
import type { FC } from 'react';
import type { ApertureProps } from './Aperture.types';

export const Aperture: FC<ApertureProps> = props => {
  return <div>{'Aperture stub'}</div>;
};
