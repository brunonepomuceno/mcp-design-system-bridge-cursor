import React from 'react';
import type { FC } from 'react';
import type { Variant_AlertProps } from './Variant_Alert.types';

export const Variant_Alert: FC<Variant_AlertProps> = props => {
  return <div>{'Variant_Alert stub'}</div>;
};
