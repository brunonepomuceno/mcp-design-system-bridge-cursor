import React from 'react';
import type { FC } from 'react';
import type { Phone_incomingProps } from './Phone_incoming.types';

export const Phone_incoming: FC<Phone_incomingProps> = props => {
  return <div>{'Phone_incoming stub'}</div>;
};
