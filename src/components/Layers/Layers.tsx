import React from 'react';
import type { FC } from 'react';
import type { LayersProps } from './Layers.types';

export const Layers: FC<LayersProps> = props => {
  return <div>{'Layers stub'}</div>;
};
