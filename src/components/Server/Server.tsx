import React from 'react';
import type { FC } from 'react';
import type { ServerProps } from './Server.types';

export const Server: FC<ServerProps> = props => {
  return <div>{'Server stub'}</div>;
};
