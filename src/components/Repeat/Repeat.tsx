import React from 'react';
import type { FC } from 'react';
import type { RepeatProps } from './Repeat.types';

export const Repeat: FC<RepeatProps> = props => {
  return <div>{'Repeat stub'}</div>;
};
