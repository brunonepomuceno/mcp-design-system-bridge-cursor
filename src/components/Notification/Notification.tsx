import React from 'react';
import type { FC } from 'react';
import type { NotificationProps } from './Notification.types';

export const Notification: FC<NotificationProps> = props => {
  return <div>{'Notification stub'}</div>;
};
