import React from 'react';
import type { FC } from 'react';
import type { Bar_chartProps } from './Bar_chart.types';

export const Bar_chart: FC<Bar_chartProps> = props => {
  return <div>{'Bar_chart stub'}</div>;
};
