import React from 'react';
import type { FC } from 'react';
import type { MonitorProps } from './Monitor.types';

export const Monitor: FC<MonitorProps> = props => {
  return <div>{'Monitor stub'}</div>;
};
