import React from 'react';
import type { FC } from 'react';
import type { MaximizeProps } from './Maximize.types';

export const Maximize: FC<MaximizeProps> = props => {
  return <div>{'Maximize stub'}</div>;
};
