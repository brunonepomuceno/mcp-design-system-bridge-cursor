import React from 'react';
import type { FC } from 'react';
import type { FooterProps } from './Footer.types';

export const Footer: FC<FooterProps> = props => {
  return <div>{'Footer stub'}</div>;
};
