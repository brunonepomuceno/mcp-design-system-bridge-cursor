import React from 'react';
import type { FC } from 'react';
import type { HeaderProps } from './Header.types';

export const Header: FC<HeaderProps> = props => {
  return <div>{'Header stub'}</div>;
};
