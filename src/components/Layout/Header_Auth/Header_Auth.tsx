import React from 'react';
import type { FC } from 'react';
import type { Header_AuthProps } from './Header_Auth.types';

export const Header_Auth: FC<Header_AuthProps> = props => {
  return <div>{'Header_Auth stub'}</div>;
};
