import React from 'react';
import type { FC } from 'react';
import type { Git_commitProps } from './Git_commit.types';

export const Git_commit: FC<Git_commitProps> = props => {
  return <div>{'Git_commit stub'}</div>;
};
