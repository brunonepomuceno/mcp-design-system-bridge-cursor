import React from 'react';
import type { FC } from 'react';
import type { HexagonProps } from './Hexagon.types';

export const Hexagon: FC<HexagonProps> = props => {
  return <div>{'Hexagon stub'}</div>;
};
