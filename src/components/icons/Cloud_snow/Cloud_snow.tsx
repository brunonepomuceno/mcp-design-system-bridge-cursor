import React from 'react';
import type { FC } from 'react';
import type { Cloud_snowProps } from './Cloud_snow.types';

export const Cloud_snow: FC<Cloud_snowProps> = props => {
  return <div>{'Cloud_snow stub'}</div>;
};
