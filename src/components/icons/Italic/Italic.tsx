import React from 'react';
import type { FC } from 'react';
import type { ItalicProps } from './Italic.types';

export const Italic: FC<ItalicProps> = props => {
  return <div>{'Italic stub'}</div>;
};
