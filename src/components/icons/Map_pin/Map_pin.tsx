import React from 'react';
import type { FC } from 'react';
import type { Map_pinProps } from './Map_pin.types';

export const Map_pin: FC<Map_pinProps> = props => {
  return <div>{'Map_pin stub'}</div>;
};
