import React from 'react';
import type { FC } from 'react';
import type { CalendarProps } from './Calendar.types';

export const Calendar: FC<CalendarProps> = props => {
  return <div>{'Calendar stub'}</div>;
};
