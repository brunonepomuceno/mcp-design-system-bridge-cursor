import React from 'react';
import type { FC } from 'react';
import type { Skip_forwardProps } from './Skip_forward.types';

export const Skip_forward: FC<Skip_forwardProps> = props => {
  return <div>{'Skip_forward stub'}</div>;
};
