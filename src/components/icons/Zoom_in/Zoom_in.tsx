import React from 'react';
import type { FC } from 'react';
import type { Zoom_inProps } from './Zoom_in.types';

export const Zoom_in: FC<Zoom_inProps> = props => {
  return <div>{'Zoom_in stub'}</div>;
};
