import React from 'react';
import type { FC } from 'react';
import type { MinimizeProps } from './Minimize.types';

export const Minimize: FC<MinimizeProps> = props => {
  return <div>{'Minimize stub'}</div>;
};
