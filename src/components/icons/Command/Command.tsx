import React from 'react';
import type { FC } from 'react';
import type { CommandProps } from './Command.types';

export const Command: FC<CommandProps> = props => {
  return <div>{'Command stub'}</div>;
};
