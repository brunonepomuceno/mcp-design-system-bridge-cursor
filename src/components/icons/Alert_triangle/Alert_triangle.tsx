import React from 'react';
import type { FC } from 'react';
import type { Alert_triangleProps } from './Alert_triangle.types';

export const Alert_triangle: FC<Alert_triangleProps> = props => {
  return <div>{'Alert_triangle stub'}</div>;
};
