import React from 'react';
import type { FC } from 'react';
import type { SpeakerProps } from './Speaker.types';

export const Speaker: FC<SpeakerProps> = props => {
  return <div>{'Speaker stub'}</div>;
};
