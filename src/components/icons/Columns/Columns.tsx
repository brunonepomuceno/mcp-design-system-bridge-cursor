import React from 'react';
import type { FC } from 'react';
import type { ColumnsProps } from './Columns.types';

export const Columns: FC<ColumnsProps> = props => {
  return <div>{'Columns stub'}</div>;
};
