import React from 'react';
import type { FC } from 'react';
import type { Hard_driveProps } from './Hard_drive.types';

export const Hard_drive: FC<Hard_driveProps> = props => {
  return <div>{'Hard_drive stub'}</div>;
};
