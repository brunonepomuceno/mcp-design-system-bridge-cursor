import React from 'react';
import type { FC } from 'react';
import type { DeleteProps } from './Delete.types';

export const Delete: FC<DeleteProps> = props => {
  return <div>{'Delete stub'}</div>;
};
