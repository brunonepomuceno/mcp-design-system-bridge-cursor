import React from 'react';
import type { FC } from 'react';
import type { GitlabProps } from './Gitlab.types';

export const Gitlab: FC<GitlabProps> = props => {
  return <div>{'Gitlab stub'}</div>;
};
