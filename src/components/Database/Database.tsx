import React from 'react';
import type { FC } from 'react';
import type { DatabaseProps } from './Database.types';

export const Database: FC<DatabaseProps> = props => {
  return <div>{'Database stub'}</div>;
};
