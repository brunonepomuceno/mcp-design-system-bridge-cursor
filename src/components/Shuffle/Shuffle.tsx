import React from 'react';
import type { FC } from 'react';
import type { ShuffleProps } from './Shuffle.types';

export const Shuffle: FC<ShuffleProps> = props => {
  return <div>{'Shuffle stub'}</div>;
};
