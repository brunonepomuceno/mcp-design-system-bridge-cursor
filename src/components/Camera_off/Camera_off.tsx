import React from 'react';
import type { FC } from 'react';
import type { Camera_offProps } from './Camera_off.types';

export const Camera_off: FC<Camera_offProps> = props => {
  return <div>{'Camera_off stub'}</div>;
};
