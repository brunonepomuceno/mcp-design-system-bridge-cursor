import React from 'react';
import type { FC } from 'react';
import type { Pricing_CardProps } from './Pricing_Card.types';

export const Pricing_Card: FC<Pricing_CardProps> = props => {
  return <div>{'Pricing_Card stub'}</div>;
};
