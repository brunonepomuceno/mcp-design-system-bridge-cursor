import React from 'react';
import type { FC } from 'react';
import type { Review_CardProps } from './Review_Card.types';

export const Review_Card: FC<Review_CardProps> = props => {
  return <div>{'Review_Card stub'}</div>;
};
