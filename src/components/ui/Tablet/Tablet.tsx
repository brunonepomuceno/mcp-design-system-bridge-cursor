import React from 'react';
import type { FC } from 'react';
import type { TabletProps } from './Tablet.types';

export const Tablet: FC<TabletProps> = props => {
  return <div>{'Tablet stub'}</div>;
};
