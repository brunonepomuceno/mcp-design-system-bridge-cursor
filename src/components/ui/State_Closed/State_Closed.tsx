import React from 'react';
import type { FC } from 'react';
import type { State_ClosedProps } from './State_Closed.types';

export const State_Closed: FC<State_ClosedProps> = props => {
  return <div>{'State_Closed stub'}</div>;
};
