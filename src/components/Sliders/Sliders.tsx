import React from 'react';
import type { FC } from 'react';
import type { SlidersProps } from './Sliders.types';

export const Sliders: FC<SlidersProps> = props => {
  return <div>{'Sliders stub'}</div>;
};
