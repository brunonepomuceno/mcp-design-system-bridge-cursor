import React from 'react';
import type { FC } from 'react';
import type { PaperclipProps } from './Paperclip.types';

export const Paperclip: FC<PaperclipProps> = props => {
  return <div>{'Paperclip stub'}</div>;
};
