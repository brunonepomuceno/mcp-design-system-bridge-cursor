import React from 'react';
import type { FC } from 'react';
import type { PackageProps } from './Package.types';

export const Package: FC<PackageProps> = props => {
  return <div>{'Package stub'}</div>;
};
