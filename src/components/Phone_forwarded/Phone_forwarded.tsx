import React from 'react';
import type { FC } from 'react';
import type { Phone_forwardedProps } from './Phone_forwarded.types';

export const Phone_forwarded: FC<Phone_forwardedProps> = props => {
  return <div>{'Phone_forwarded stub'}</div>;
};
