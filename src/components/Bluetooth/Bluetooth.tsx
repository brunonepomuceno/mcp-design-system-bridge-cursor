import React from 'react';
import type { FC } from 'react';
import type { BluetoothProps } from './Bluetooth.types';

export const Bluetooth: FC<BluetoothProps> = props => {
  return <div>{'Bluetooth stub'}</div>;
};
