import React from 'react';
import type { FC } from 'react';
import type { DialogProps } from './Dialog.types';

export const Dialog: FC<DialogProps> = props => {
  return <div>{'Dialog stub'}</div>;
};
