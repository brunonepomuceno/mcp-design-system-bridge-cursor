import React from 'react';
import type { FC } from 'react';
import type { Form_ContactProps } from './Form_Contact.types';

export const Form_Contact: FC<Form_ContactProps> = props => {
  return <div>{'Form_Contact stub'}</div>;
};
