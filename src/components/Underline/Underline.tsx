import React from 'react';
import type { FC } from 'react';
import type { UnderlineProps } from './Underline.types';

export const Underline: FC<UnderlineProps> = props => {
  return <div>{'Underline stub'}</div>;
};
