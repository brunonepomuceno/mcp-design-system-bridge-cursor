import React from 'react';
import type { FC } from 'react';
import type { Stop_circleProps } from './Stop_circle.types';

export const Stop_circle: FC<Stop_circleProps> = props => {
  return <div>{'Stop_circle stub'}</div>;
};
