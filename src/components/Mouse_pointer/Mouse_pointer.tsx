import React from 'react';
import type { FC } from 'react';
import type { Mouse_pointerProps } from './Mouse_pointer.types';

export const Mouse_pointer: FC<Mouse_pointerProps> = props => {
  return <div>{'Mouse_pointer stub'}</div>;
};
