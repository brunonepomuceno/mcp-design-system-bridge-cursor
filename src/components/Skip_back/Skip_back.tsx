import React from 'react';
import type { FC } from 'react';
import type { Skip_backProps } from './Skip_back.types';

export const Skip_back: FC<Skip_backProps> = props => {
  return <div>{'Skip_back stub'}</div>;
};
